import { Settings } from '../types/settings.interface';

export const DEFAULT_SETTINGS: Settings = {
  blockSize: {
    width: 30,
    height: 30
  },
  wallThickness: 2,
  pathThickness: 2,
  showPaths: true
}